import { ctaProps, sectionProps } from '@/interfaces'
import React from 'react'

const CTA = ({className, ctaLabel, ctaLink, title, description}: ctaProps) => {
  return (
    <section className={`${className}`}>
        KONTOL KECIK
    </section>
  )
}

export default CTA